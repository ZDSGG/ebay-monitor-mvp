# Frontend

阶段 5 已初始化 React + Vite 前端。

页面：

- `/items` 商品列表
- `/items/new` 新增商品
- `/items/:itemId` 商品详情

启动：

```bash
cd frontend
npm install
npm run dev
```

可选环境变量：

```bash
VITE_API_BASE_URL=http://localhost:8000/api
```
